{
  "scripts": {
    "startlocal": "truffle develop",
    "test": "truffle migrate --reset --network test && truffle test --network test",
    "testlocal": "truffle migrate --reset --network develop && truffle test --network develop"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2",
    "mocha": "^9.1.1",
    "mocha-junit-reporter": "^2.0.0",
    "mocha-multi-reporters": "^1.5.1",
    "truffle": "^5.4.9",
    "truffle-assertions": "^0.9.2",
    "web3": "^1.5.2"
  }
}
